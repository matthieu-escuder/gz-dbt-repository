version: 2

sources:
  - name: raw
    schema: gz_raw_data
    tables:
      - name: sales
        schema: gz_raw_sales
      - name: product
        schema: gz_raw_product
      - name: ship
        schema: gz_raw_ship
